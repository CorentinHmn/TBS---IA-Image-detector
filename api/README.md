---
title: TBS IA Image Detector API
emoji: 🔍
colorFrom: blue
colorTo: purple
sdk: docker
pinned: false
---

# TBS AI Image Detector — FastAPI Backend

POST `/analyze` — multipart image → JSON with `aiProbability`, `riskLevel`, `signals`

GET `/health` — returns model load status
